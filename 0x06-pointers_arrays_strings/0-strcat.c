#include "main.h"
#include <string.h>
/**
 * _strcat -  concatenates two strings
 * @dest: strings to be concatenated
 * @src: source string
 *
 * Return: dest string
 */
char *_strcat(char *dest, char *src)
{
strcat(dest, src);
return (dest);
}
