#include <ctype.h>
#include "main.h"
/**
 * _isdigit - checks whether a character is a digit
 * @c: character to be checked
 *
 * Return: 1 (is id digit)
 */
int _isdigit(int c)
{
if (isdigit(c))
{
return (1);
}
else
{
return (0);
}
return (0);
}
